import 'package:flutter/material.dart';

class CourseFileOutcome extends StatelessWidget {
  const CourseFileOutcome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title : Text("Course File Outcome")),
      body: Center(child: Text("Course File Outcome")),
    );
  }
}
